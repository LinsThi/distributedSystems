from server.utils import login

if __name__ == '__main__':
    while True:
      # username = input("Digite seu nome de usuário: ")
      # password = input("Digite sua senha: ")
      # login(username, password)

      # login('antonio', '123')
      login('admin', '123')
      break